<UserControl x:Class="PrimalEditor.Editors.TransformView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PrimalEditor.Editors"
             xmlns:util="clr-namespace:PrimalEditor.Utilities.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource LightTextBlockStyle}"/>
    </UserControl.Resources>
    <local:ComponentView Header="Transform ">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="78"/>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Position" Grid.Column="0" Grid.Row="0"/>
            <util:VectorBox X="{Binding PosX}" Y="{Binding PosY}" Z="{Binding PosZ}" Grid.Column="1" Grid.Row="0"
                            PreviewMouseLeftButtonDown="VectorBox_PreviewMouseLeftButtonDown"
                            PreviewMouseLeftButtonUp="VectorBox_PreviewMouseLeftButtonUp"
                            LostKeyboardFocus="VectorBox_LostKeyboardFocus"/>

            <TextBlock Text="Rotation" Grid.Column="0" Grid.Row="1"/>
            <util:VectorBox X="{Binding RotX}" Y="{Binding RotY}" Z="{Binding RotZ}" Grid.Column="1" Grid.Row="1"
                            PreviewMouseLeftButtonDown="VectorBox_PreviewMouseLeftButtonDown_1"
                            PreviewMouseLeftButtonUp="VectorBox_PreviewMouseLeftButtonUp_1"
                            LostKeyboardFocus="VectorBox_LostKeyboardFocus_1"/>

            <TextBlock Text="Scale" Grid.Column="0" Grid.Row="2"/>
            <util:VectorBox X="{Binding ScaleX}" Y="{Binding ScaleY}" Z="{Binding ScaleZ}" Grid.Column="1" Grid.Row="2"
                            PreviewMouseLeftButtonDown="VectorBox_PreviewMouseLeftButtonDown_2"
                            PreviewMouseLeftButtonUp="VectorBox_PreviewMouseLeftButtonUp_2"
                            LostKeyboardFocus="VectorBox_LostKeyboardFocus_2"/>
        </Grid>
    </local:ComponentView>
</UserControl>
